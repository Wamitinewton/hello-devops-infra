apiVersion: v1
kind: Service
metadata:
  name: hello-devops
  namespace: production
  labels:
    app: hello-devops
spec:
  type: NodePort
  selector:
    app: hello-devops
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080